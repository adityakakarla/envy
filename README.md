## Envy

Envy is a CLI tool that solves a simple problem: generating .env.example files.

Simply navigate to your terminal and execute 'envy run' to automatically build a .env.example file from your .env file.

### Installation
```cargo install --git https://github.com/adityakakarla/envy```

### Usage
```envy run```
